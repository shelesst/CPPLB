#include <iostream>
#include <string>
#include <math.h>
#define _USE_MATH_DEFINES

int main()
{
  int a = 2;
  double b = 11.05;
 
 double result = 0;
 result = (sqrt(3) * (((0.1) * ( log (M_E*(3)))) / ((sin(2))*(a/b)*M_PI)+(cos(2)*((a/b)*M_PI))));
 std:: cout << result;
}